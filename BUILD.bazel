load("@rules_zig//zig:defs.bzl", "zig_binary")
load("@rules_zig//zig/zls:zls_completion.bzl", "zls_completion")

zig_binary(
    name = "zml_diffusion",
    srcs = glob(["src/**/*.zig"]),
    main = "src/main.zig",
    deps = [
        "@zigimg",
        "@zml//zml",
    ],
)

zls_completion(
    name = "completion",
    deps = [":zml_diffusion"],
)
